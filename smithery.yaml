# Smithery configuration for radiomcp
# https://smithery.ai/docs/build/project-config/smithery-yaml
runtime: "container"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties: {}
